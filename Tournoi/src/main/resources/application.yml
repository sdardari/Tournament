spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/DbPadel
    username: postgres
    password: Mxq/73El
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 5050
