spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_tournament
    username: postgres
    password: 159159
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 7070
